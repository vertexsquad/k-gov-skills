[project]
name = "k-gov-skills"
version = "0.0.0"
requires-python = ">=3.14,<3.15"

[dependency-groups]
dev = ["ruff==0.15.10"]

[tool.uv]
package = false
required-version = "==0.12.5"

[tool.ruff]
required-version = "==0.15.10"
target-version = "py314"
# Keep Ruff's default E4, E7, E9 and F rules.
